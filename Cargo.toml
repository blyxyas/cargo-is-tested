[package]
name = "is_tested"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
syn = {version = "1.0.107", features = ["full", "extra-traits"]}

[workspace]
members = ["is_tested_plugin", "testing"]